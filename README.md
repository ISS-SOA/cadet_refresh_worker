## Cadet Refresh Worker

HTTP GET requests a URL from an SQS queue

### Setup
Create `config/worker.yml` file from `config/worker.yml` template and
fill in credentials

### Deployment

    $ rake deploy
